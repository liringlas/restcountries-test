import styled from 'styled-components';

export const HeaderHeading = styled.h1`
    color: white;
    font-size: 32px;
    line-height: 1.4;
    margin: 0;
    flex: 1 0 100%;

    a {
        color: white;
        text-decoration: none;
        cursor: pointer;

        &:visited {
            color: white;
        }
    }
`