import styled from 'styled-components';
import { cssVars } from './vars';

export const RegionsList = styled.ul`
    list-style-type: none;
    border-left: 2px solid ${cssVars.color.light};
    margin: 0 0 24px 0;
    padding: 0 0 0 24px;
    width: 100%;
`