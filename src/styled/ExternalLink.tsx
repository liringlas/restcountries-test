import styled from 'styled-components';

export const ExternalLink = styled.a`
    color: white;

    &:visited {
        color: white;
    }

    margin: 0 12px;

    &:only-child {
        margin: 0;
    }
`